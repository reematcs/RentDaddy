# Clerk Authentication
CLERK_SECRET_KEY=sk_test_your_secret_key
CLERK_WEBHOOK=whsec_your_webhook_secret

# Admin Clerk ID (for automatic admin setup)
ADMIN_CLERK_ID=user_your_clerk_id

# OpenAI Integration (optional for chatbot)
OPENAI_API_KEY=your_openai_key

# Server Configuration
PORT=8080
DOMAIN_URL=http://localhost
TEMP_DIR="/app/temp"

# Database Configuration
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppassword
POSTGRES_DB=appdb
POSTGRES_PORT=5432
POSTGRES_HOST=postgres
PG_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?sslmode=disable

# Admin Credentials
ADMIN_FIRST_NAME=First
ADMIN_LAST_NAME=Landlord
ADMIN_EMAIL=admin@example.com

# SMTP Configuration
SMTP_PORT=587
SMTP_ENDPOINT_ADDRESS=email-smtp.example.com
SMTP_USER=your_smtp_username
SMTP_PASSWORD=your_smtp_password
SMTP_TLS_MODE=starttls
SMTP_FROM=noreply@example.com
SMTP_TEST_EMAIL=test@example.com

# Documenso Integration
DOCUMENSO_HOST=documenso
DOCUMENSO_PORT=3000
DOCUMENSO_API_URL=http://${DOCUMENSO_HOST}:${DOCUMENSO_PORT}
DOCUMENSO_PUBLIC_URL=http://localhost:3000
DOCUMENSO_API_KEY=api_your_documenso_key
DOCUMENSO_WEBHOOK_SECRET=your_webhook_secret
DOCUMENSO_SIGNING_PASSPHRASE=teamezraapp

# Cron Job Authentication
CRON_SECRET_TOKEN=your_secure_random_token

# Application Environment
ENV=development
DEBUG_MODE=false

# Authentication for tests
BEARER_TOKEN=your_test_bearer_token

# Development tools
NGROK_AUTHTOKEN=your_ngrok_auth_token
USE_AIR=true